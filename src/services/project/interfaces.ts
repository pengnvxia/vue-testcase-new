export interface Project {
    id: number,
    projectName: string,
    devAddress: string,
    prodAddress: string,
    description: string
}

export interface Module {
    id: number,
    moduleName: string
}
