export interface Group{
    id: number,
    groupName: string,
    projectId: number,
    projectName: string,
    envId: number,
    updatedBy?: string,
    updatedAt?: string,
    description?: string
}

